name: Workflow
on:
  push:
    branches: [ change-go-version-gosec ]
  pull_request:
    branches: [ change-go-version-gosec ]
jobs:
  code-check:
    name: Check Go formatting, linting, vetting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Run the formatter, linter, and vetter
        uses: dell/common-github-actions/go-code-formatter-linter-vetter@main
        with:
          directories: ./...
  test:
    name: Run Go unit tests and check package coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout csi-powerflex
        uses: actions/checkout@v2
      - name: Run unit tests and check package coverage
        uses: dell/common-github-actions/go-code-tester@main
        with:
          threshold: 90
          test-folder: "./service"
  gosec:
    name: Run gosec
    runs-on: ubuntu-latest
    steps:
      - name: Checkout csi-powerflex
        uses: actions/checkout@v2
      - name: Set goflags
        uses: dell/common-github-actions/gosec-runner@gosec-temp-fix
      - name: Run Go Security
        uses: securego/gosec@master
        with:
          args: -exclude=G304 ./...
#  go_security_scan:
#    name: Go security
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout the code
#        uses: actions/checkout@v2
#      - name: Set weird git flag to false
#        run: echo "{GOFLAGS}={-buildvcs=false}" >> $GITHUB_ENV
#      - name: Set -buildvcs=false to false
#        run: echo "-buildvcs=false" >> $GOFLAGS
#      - name: print GITHUB_ENV
#        run: echo ${{ env.GITHUB_ENV }}
#      - name: print GOFLAGS
#        run: echo ${{ env.GOFLAGS }}
#      - name: Run Go Security
#        uses: securego/gosec@master
#        with:
#          args: -exclude=G304 ./...
  malware_security_scan:
    name: Malware Scanner
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Run malware scan
        uses: dell/common-github-actions/malware-scanner@main
        with:
          directories: .
          options: -ri
