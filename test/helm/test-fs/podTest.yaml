kind: Pod
apiVersion: v1
metadata:
  name: pod-test
  namespace: helmtest-vxflexos 
spec:
#  securityContext:
 #  runAsNonRoot: true
 #  runAsUser: 54321
 #  fsGroup: 54321
  containers:
    - name: my-frontend
      image: busybox
      command: [ "sleep", "100000" ]
      volumeMounts:
      - mountPath: "/data0"
        name: pvol0
  volumes:
    - name: pvol0
      persistentVolumeClaim:
        claimName: pvol0
  nodeSelector:
      kubernetes.io/hostname: worker-1-ttd5oqydzb39f
