apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: block-pvc
  namespace: helmtest-vxflexos
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: vxflexos
  volumeMode: Block
  #volumeName: block-pv
